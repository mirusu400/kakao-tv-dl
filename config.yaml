throttle: { sleep_min: 2, sleep_max: 6, concurrency: 2 }
quality:  { max_height: 1080 }
search:   { size: 20, max_pages: 200 }
exclude:  { channels: [], keywords: ["뉴스", "news"] }
ia:
  auto_upload_policy: "own_orphan_cc_only"
  collection: "opensource_movies"
